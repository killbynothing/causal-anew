# AGENTS.md —— 全局规则与约束（新仓 · 2026-08-03 起）

> 每次开新会话**最先读本文件 + STATUS.md**。只放规则、红线、路由、命令；叙事/进度/历史一律进 STATUS.md 和 docs/。
> 超过 ~120 行触发一次「洁癖」精简。

---

## 项目一句话

把已完结长篇《存在的意义：因果之外》重构成「AI 原生」叙事游戏。后台既是**数字人 NPC**又是**导演**，把玩家自由文本拉回原著正典节点。核心哲学：**导演 = 剧中反派**。

**北极星**：真造一个持续存在、角色会记忆、会判断、会主动行动并彼此影响的世界。机制、长上下文和 δ 都服务于角色活着，不是目的本身。

---

## 不可违背的红线

1. **库赢**：`data/world_truth.db` 是唯一真值源；与库冲突的任何文本以库为准。
2. **run=0 只读 / run≥1 只追加**；三轴不混：`worldline`(剧情宇宙) / `run`(周目) / `ch_anchor`(时序)。
3. **never_soften 固定底绝不松**；确定性红线闸不走 LLM。固定底=因果线收束（`causal_web`），救它=同拍拆全网。
4. **四支柱以编译报错存在**：Ⅰ`E_SELF_REF` Ⅱ`E_XLINE_CHANNEL` Ⅲ`E_FREE_INTERVENTION` Ⅳ`E_COHERENCE_PHASE`。
5. **禁旁白硬拽**：撞红线只能剧情内收敛或现实物理收敛。
6. **Goodhart 护栏**：不为变绿删测试/删用例/删禁忌词/放宽阈值。导演只能接受「可行新解法并命名为合法 δ 路径」。
7. **★★★ 创意裁决不下放**：节点 tier、canon_locked 升降级、主角行动模型、张尘弧光等，Agent 只出草案标 ★★★，人裁。
8. **圣经/卡是库的投影**：不可手改渲染产物反向污染库；改库再重渲染。
9. **内容三铁律（2026-08-02 亲裁）**：**不许编**（入库内容必出自库/原著、带 canon_src 行号）· **可审**（入库前 diff 预览、人点头才入）· **报账**（每大 loop 报告单列「哪里是我编的」，目标=零）。

---

## 文件落位（新仓防乱的唯一机制）

| 类型 | 去哪 | 备注 |
|---|---|---|
| 真值库 | `data/world_truth.db` | 唯一；导入源另列 |
| 契约/语料/原著 | `contracts/` `corpus/` `source/` | 库的上游 |
| 当前进度 | `STATUS.md` | 日报式，新的在最上 |
| 计划 | `docs/plans/` | **必须登记 INDEX.md 才算存在** |
| 设计 | `design/` | 角色装配、导演闸、三层分工 |
| **游玩记录** | `play_logs/YYYY-MM/` | 纳入 git（是证据）；每月一文件夹 |
| **人的体验记录/想法** | `docs/experience/` | 与 play_logs 分开：这是人写的 |
| 过程稿/临时 | `docs/` | 不堆根目录 |
| 参考材料 | `docs/refs/` | 外部文章/开源笔记 |
| 一次性脚本 | `scratch/` | 用完即弃 |
| 归档 | `_archive/` | 只进不出；**删任何东西先出清单人审** |
| 运行时 / 验证 | `runtime/` `scripts/` `web/` | |

**禁**：根目录新增散文件（允许的只有 AGENTS/STATUS/README/WORKFLOW 等规则件）。

---

## 怎么给 AI 下指令（用户 cheat sheet）

1. **只说目标和验收**：「把 X 做到 Y，用 Z 验证」，不说步骤——步骤是它的事。
2. **要求 loop 定义块**：目标(机器可验证)/边界/降级/验证命令/心跳/做-验分开。没有这块不开工。
3. **开工前让它读 STATUS.md**；它拿不准去哪找东西，让它读本文件「文件落位」。
4. **收尾四件套**：`--quick` 绿 + 报账（哪里是它编的）+ STATUS 更新 + 计划文档同步。缺一样不算完。
5. **内容自审自验**：端给人裁/人看之前，Agent 必须按 `角色装配备忘` 自验层放对、有出处、不焊死成长；**不合格不准端上来**。
6. **它跑偏时**：贴红线编号（「撞红线 9」），不重述需求。
7. **内容类工作**：它只能出草案标 ★★★，你过目后才入库——这条撞了直接停。
8. **多 AI 并行**：一个 loop 一个分支 `loop/<名>`，各干各的，收尾 squash 合回。

---

## Git 规范（从简但硬）

- `main` 永远绿；**绝不** rebase / force-push。
- 一个 loop 一个分支 `loop/<名>`；收尾 squash 合回；`--quick` 绿才许合。
- commit 信息：`<loop>: <做了什么>`，一行说人话。
- 每 loop 收尾提交；游玩记录也提交（进 `play_logs/`）。
- pre-commit 红灯不许 `--no-verify`。

---

**远程**：`https://github.com/killbynothing/causal-anew`（`origin`）。特性分支 `loop/<名>` 推这里；`main` 只 squash 合。

## 关键命令

```bash
python scripts/verify.py --quick            # 心跳总闸（必绿）
python scripts/verify.py --full             # 含 LLM/耗时的
python scripts/mech_invariant_suite.py --db data/world_truth.db   # 四支柱 12/12
python scripts/import_db.py                 # 建表+入库（动库唯一通道）
python scripts/generate_bibles.py           # 从库重渲染投影产物
```

---

## 文件路由

| 想知道 | 看 |
|---|---|
| 长期排期（唯一执行入口） | `docs/plans/长期路线图_2026-08-03.md` |
| 角色怎么装（用什么/可变层） | `design/角色装配备忘_用什么×怎么装×可变层_2026-08-03.md` |
| 人格底色 / 怎么装 | `design/角色装配备忘_用什么×怎么装×可变层_2026-08-03.md` |
| 人格/关系人裁格式 | `docs/plans/人裁模板_人格核×关系图_2026-08-03.md` |
| 当前真相/进度 | `STATUS.md` |
| 计划索引 | `docs/plans/INDEX.md` |
| 怎么干活（loop 格式） | `LOOP总纲_LOOP_CHARTER.md`（待搬）/ 旧仓同名文件 |
| Git 纪律 | `docs/git-workflow.md` |
| 旧仓（封存只读） | 桌面目录 `令人充满希望的进行啊`（双根工作区） |

---

## 核心词汇（最小集）

- **意识 vs 身体**：记忆绑意识、换壳不带走。
- **δ 偏离账本**：玩家行动制造的偏离；终幕清单 100% 来自玩家真实记录。
- **节点契约 = storylet**：固定事实层 + 导演演绎层；事件由导演在骨架内排布。
- **世界软化 S(node)**：跨周目沉淀（先例/痕迹/解锁/见证）磨低收敛阻力，单调有界永不归零；固定底恒 ∞。
- **数字人六件套**：人格核不可覆写 / 知识门控 / FSM暴露层级 / 慢环检索绑意识 / Meta_State 双通道 / 渗漏免疫。

---

*本文是规则不是文档。任何与 `world_truth.db` 冲突的表述，以库为准。*
