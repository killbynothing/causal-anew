# Ch22 无离散事件说明

> 登记用途：满足 `verify_truth_completeness.py` 的 `ch_gap_registry`；本章在原著中存在，但细剖事件表未拆出独立 `event_uid` 行。

## 章节存在性

- `source/chapters.json` 键 `"22"` 有正文（柳絮、修哉、魏初餐厅对话，约日常闲聊/氛围段）。
- 全书细剖覆盖 Ch1–131 中 **129** 个有事件章节；Ch22 与 Ch47 为**有章无表**缺口。

## 为何无 event 行

- 本章内容为角色日常对话与氛围铺陈，未形成可单独落库的因果推动拍（无新地点转移、无契约级结果锁、无跨线知识变动需入账）。
- 细剖生产线按「可落 `events(run=0)` 的原子拍」标准，整章保留在原著连续文本中，不强行拆伪事件。

## 源锚

- 原著：`source/chapters.json` → `"22"`（餐厅柳絮/修哉/魏初段）。
- 若后续人裁认定本章某拍应升格为可玩节点，须先补细剖表行再转录，禁止仅在卡面引用未入库 uid。
