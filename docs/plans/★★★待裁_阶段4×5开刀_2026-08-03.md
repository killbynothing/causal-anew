# ★★★ 阶段4/5 开刀前裁决 · 2026-08-03

> 你说「阶段45都做了」。机器侧能立刻动手，但 **β 两表建表 = schema 变更**，红线要你先点头。

## 阶段 4 要做的（S5–S7）

| 块 | 内容 | 卡你的点 |
|---|---|---|
| S5 导演参数 | 收敛参数/宪章入库 | 内容 ★★★ 可审 |
| S6 β 物理层 | `run_meta` + `delta_sediment` + `settle_run.py` | **建表=schema**；规格见旧仓 `design/10_玩家层/β跨周目…v0.2` §四 §九 |
| S7 卡渲染 | `generate_cards.py` + `cards_regen_parity` | 终态大；可先只对两场卡做投影对等 |

## 阶段 5 要做的（S8–S10）

| 块 | 内容 | 卡你的点 |
|---|---|---|
| S8 人验 | 你正在做：两场观测台 | **你验** |
| S9 观测台 | beat_io 已补；β/沉淀视图待投影 | 跟 S6 |
| S10 causal_web | 固定底人×事×机制×章 | **★★★ 逐链人裁**，不能代裁 |

## 裁决记录（2026-08-03）

| # | 题 | 裁 |
|---|---|---|
| 1 | 建 `run_meta` / `delta_sediment` | ✅ 批准；已落表 + `settle_run.py` + `beta_sediment_schema` 验证器 |
| 2 | 权重 0.25 / 0.10 / S_max 0.6 | ✅ 按草案；`runtime/softening_params.py` |
| 3 | causal_web | ⏳ 草稿 v2（医院脊柱=视频→追杀→受伤入院；苏颖固定底已删） |

### 题 3 仍待你点

见草案文末 §6：粒度 / 医院 sink / strength / 固定底是否本阶段不填边 / 样板是否入库。
