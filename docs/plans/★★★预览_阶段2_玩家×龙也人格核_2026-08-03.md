# ★★★ 草案预览：阶段2 玩家壳 + 龙也人格核（已入库，等人验）· 2026-08-03

> 按指示先全做完再验。零新剧情：玩家基线来自已裁计划；龙也语句来自序幕卡 / persona md / persona_constraints。

## S2 玩家壳（Seed）

| 行 | 内容 |
|---|---|
| archetypes.player | 玩家（可扮演壳） |
| B.player | human / W-MAIN |
| C.player | jump_capable=0 |
| occupancy | B.player×C.player native |
| P.PLAYER.ORDINARY_SHELL | 普通人壳、无跳线 |
| P.PLAYER.RUN_PROFILE_NOT_SEED | 姓名等只在 run.player_profile |
| P.PLAYER.BODY_CONS_PAIR | 壳对说明 |

开档设定（阿七/年龄/身份）**不进 Seed**，仍走 session `player_profile`。

## S3 龙也人格核（仅 C.ryuya.W1）

- `P.VOICE.ryuya.W1.001–004` ← 卡 voice_samples
- `P.BOUNDARY.ryuya.W1.hard/soft/*` + `iron_law` ← 卡 boundaries + persona_constraints
- `P.MANNER.ryuya.W1.style/constraint.*/persona_md` ← 腔调/本性约束/persona md

卡侧：`voice_samples=[]`，boundaries 改 seed id 列表；保留 2 条场次约束（托付全名、交挂坠）。

## 报账

- **零编造剧情**。同行/托付类未新增。
- 家人/禁泄内容沿用卡与铁律原文。
- 微调：改 `scripts/migrate_persona_core_ryuya.py` / `migrate_player_seed.py` 后重跑。

## 你怎么验

1. `persona_core_parity` / `player_packet_projection` 已绿（`--quick`）。
2. **玩一局龙也序幕，判「龙也还是龙也」**——只有你能验。
