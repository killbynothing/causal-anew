# 长期路线图（新仓唯一执行入口）· 2026-08-03

> **性质**：新仓唯一排期入口。旧仓《AI原生叙事长期总计划_2026-07-23》降为证据；执行细节沿用《初始库重建×全投影_详细计划_2026-08-01》§11 执行手册（S0–S10）与《新馆搬迁方案_库优先×精选迁移_2026-08-02》（M0–M5）。
> **读法**：每个阶段只列「做什么 / 你验什么」——你只看灯，不看过程。🔴 任一验证不过，阶段不算完。
> **总锚点**：**阶段 0 结束 = 到现在能玩的程度**（Demo 现状平移到新仓）；阶段 2 结束 = 龙也序幕完全由库驱动（超过现在）；阶段 4 结束 = 全投影（手写卡全部退役）。

---

## 阶段 0 · 搬迁（M0–M5）——「到现在能玩的程度」

| 步骤 | 做什么 | 状态 |
|---|---|---|
| M0 | 新仓 git init + AGENTS.md（治理规则）+ 本路线图 | ✅ 2026-08-03 |
| M1 | 搬库：`data/world_truth.db` + 契约/语料/原著 + 导入源 | ⬜ |
| M2 | 搬验证基建：`verify.py` + 四支柱/契约/只读验证器先挂 | ⬜ |
| M3 | 搬 runtime + 精选 scripts（~20 个）+ 修写死路径 | ⬜ |
| M4 | Demo 冒烟：龙也序幕 + 天安门各跑一拍 | ⬜ |
| M5 | 旧仓打 tag `archive/pre-move-2026-08-03` 封存只读 | ⬜ |

**你验什么**：① 四支柱 `mech_invariant_suite` 12/12 绿（=库没搬坏）；② `--quick` 绿；③ 你亲自玩一拍龙也序幕，感觉和旧仓一样。**三条全过 = 到现在能玩的程度。**

## 阶段 1 · 初始库地基（S0–S1：登记簿 + 身份/物件）

做什么：扫描全部场卡字段 → 登记簿 csv；起草开场 5 意识 `REL.IDENTITY`（★★★ 你过目）入库；items 补属性、清 run 域陈述；卡改引用 Seed ID。

**你验什么**：① 登记簿给你抽查前 20 行；② `seed_identity_projection` / `seed_no_run_facts` 绿；③ 玩龙也序幕：弟弟/表姐/挂坠来历这些**来自库**，卡里没有重复手写。

## 阶段 2 · 玩家 + 龙也竖切（S2–S3 前半）

做什么：`B.player`/`C.player` 注册（★★★ 你过目）+ 开档设定进 run 域；**只做龙也一人**的人格核（腔调/底线/口癖 → 库，每条带原著行号，★★★ 你过目）；卡改引用。

**你验什么**：① `persona_core_parity` 绿；② **你玩一局龙也序幕，判「龙也还是龙也」**——这条只有你能验。

## 阶段 3 · 人格核量产 + 场卡入库（S3 后半 + S4）

做什么：龙也管线复制到修哉/真纪/卡卡西/秋人（再魏初）；场卡场景级字段进 `node_contracts`（固定事实层 + 导演演绎层）；身体档案/动作类型入库。

**你验什么**：① 卡内临时补丁字段清零（验证器断言）；② `storylet_projection_parity` 绿；③ 天安门一局：四人口吻、知识门控无泄漏。

## 阶段 4 · 导演参数 + β 物理层 + 卡渲染化（S5–S7）

做什么：导演收敛参数/宪章入库；`run_meta` + `delta_sediment` 建表 + 结算脚本；`generate_cards.py`——卡变成库的渲染产物，runtime 切换读取源。

**你验什么**：① `cards_regen_parity` 绿（渲染卡 ≡ 旧卡）；② 沉淀/软化/witness 13 个验证器绿；③ 连开两局：第二局开局时给你看**结算单**（上一局哪些 δ 成了先例/痕迹/解锁/见证）。

## 阶段 5 · 人验 + 观测台 + 因果网（S8–S10）

做什么：全流程人验；观测台重构（四种包 + 沉淀视图全链路可见）；固定底因果网 `causal_web` 从原著抽链（人×事×机制×章，**逐链 ★★★ 你过目**）。

**你验什么**：① 人验清单全过（像世界在长，不是纯聊天）；② 观测台里能看到每个包从库哪行来；③ 因果网每条链你点头过。

---

## 平行长期线（不挡主线，插空做）

| 线 | 内容 | 何时 |
|---|---|---|
| 情绪引擎 | `affect_state` 启用 + 七情 V/A 词表 + 每拍后情绪审计；本地模型期接 persona vectors 测漂移 | 阶段 3 后 |
| 演出质感 | 文字风格/群演调用/悲壮氛围（挂坠弃置后果等） | 持续 |
| 开源消化 | MISS（AGPL 只借鉴）/ Airis（MIT 可借）核心文件补抓：miss_system.j2、attribute_engine.py、heart_system.py | 网络通时 |

## 纪律（每阶段通用）

1. 验证器先写，实现后写；每 loop 收尾 `--quick` 绿 + 报账 + STATUS 更新。
2. 内容草案一律 ★★★ 人过目才入库（红线 9：不许编/可审/报账）。
3. 动库只走 import 管线；迁移给 before/after diff。
4. 阶段结束触发一次知识洁癖：精简 AGENTS/STATUS/docs，归档而非删除。

## 与旧文档关系

| 文档 | 关系 |
|---|---|
| 旧仓长期总计划 2026-07-23 | 证据；本图接棒唯一执行入口 |
| 初始库重建×全投影_2026-08-01 | 阶段 1–5 的执行细则（S0–S10 + §11 手册） |
| β 机制设计 v0.2.1 | 阶段 4 的规格（§十一含全部人裁） |
| 新馆搬迁方案 2026-08-02 | 阶段 0 的细则（N1–N3 已裁） |
